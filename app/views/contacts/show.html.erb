<%= stylesheet_link_tag 'base'%>

<div class="wrapper-show-contact">
  <p style="color: green"><%= notice %></p>

  <%= render @contact %>

  <div>
    <%= link_to "Editar Contato", edit_contact_path(@contact), class: "btn btn-primary" %> 
    <%= link_to "Home", home_index_path, class: "btn btn-secondary" %>
  </div>

</div>
